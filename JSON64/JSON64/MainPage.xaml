<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="JSON64.MainPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <StackLayout>
        <Frame
            Padding="24"
            BackgroundColor="#2196F3"
            CornerRadius="0">
            <Label
                FontSize="36"
                HorizontalTextAlignment="Center"
                Text="Welcome to Xamarin.Forms!"
                TextColor="White" />
        </Frame>
        <Label
            Padding="30,10,30,10"
            FontSize="Title"
            Text="Start developing now" />
        <Label
            Padding="30,0,30,0"
            FontSize="16"
            Text="Make changes to your XAML file and save to see your UI update in the running app with XAML Hot Reload. Give it a try!" />

        <Label Text="Usuario" />
        <Entry Text="{Binding Username}" />

        <Label Text="Contraseña" />
        <Entry IsPassword="True" Text="{Binding Password}" />

        <Label Text="Nombre" />
        <Entry Text="{Binding Name}" />

        <Label Text="Apellido 1" />
        <Entry Text="{Binding AP1}" />

        <Label Text="Apellido 2" />
        <Entry Text="{Binding AP2}" />

        <Label Text="{Binding JsonResult}" />

        <Button Command="{Binding RegisterCommand}" Text="Login" />

    </StackLayout>

</ContentPage>
